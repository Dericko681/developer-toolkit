---
# Default versions for CLI tools.
# These values are used if not overridden by group_vars/all.yml or other higher-precedence variables.
# Set to 'latest' to always get the most recent stable version.

jq_version: "latest" # Example: "1.6"
yq_version: "latest" # Example: "4.35.2"