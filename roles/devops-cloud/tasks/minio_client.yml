# roles/devops_cloud_native/tasks/minio_client.yml
---
- name: Download MinIO Client binary
  ansible.builtin.get_url:
    url: "https://dl.min.io/client/mc/release/linux-amd64/mc"
    dest: "/usr/local/bin/mc"
    mode: '0755'
  tags:
    - minio-client